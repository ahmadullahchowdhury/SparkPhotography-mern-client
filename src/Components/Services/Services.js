import React from 'react';
import useTitle from '../Hooks/hooks';

const Services = () => {
    useTitle('Services')
    return (
        <div>
            <h1>service page</h1>
        </div>
    );
};

export default Services;