import React from 'react';
import useTitle from '../Hooks/hooks';

const MyReviews = () => {
    useTitle('My Reviews')
    return (
        <div>
            <h1>review page</h1>
        </div>
    );
};

export default MyReviews;